<div class="container">
  <div class="row main-div">
    <div class="col-xs-12 col-sm-6">
      <div class="logo">{{#link-to 'index'}}screenwritr{{/link-to}}</div>
    </div>
    <div class='col-xs-12 col-sm-6 text-right header-name'>
      {{#if session.isAuthenticated}}
        {{session.user.firstName}}
      {{/if}}
    </div>
    <div class="col-xs-12">
      {{outlet}}
    </div>
  </div>
</div>
