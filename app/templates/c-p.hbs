<div class='row'>
  <div class='col-xs-12'>

    <div class='control-panel'>
      {{#if session.isAuthenticated}}
        {{partial 'c-p/user-panel'}}
      {{else}}
        {{partial 'c-p/guest-panel'}}
      {{/if}}
    </div>

  </div>
</div>
