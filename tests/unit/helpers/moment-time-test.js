import {
  momentTime
} from 'screenwritr/helpers/moment-time';

module('MomentTimeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = momentTime(42);
  ok(result);
});
