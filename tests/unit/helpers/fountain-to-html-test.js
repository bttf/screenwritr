import {
  fountainToHtml
} from 'screenwritr/helpers/fountain-to-html';

module('FountainToHtmlHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = fountainToHtml('test');
  ok(result);
});
